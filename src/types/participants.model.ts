export type Participant = {
  legalName: string,
  claimsGraphUri: string[],
  uri: string,
  format?: string,
  labels: string[],
  gaiaxTermsAndConditions: string,
  //description?: string,
  // legalForm?: string,
  // registrationNumber?: string,
}
